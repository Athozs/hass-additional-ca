
# Loads default set of integrations. Do not remove.
default_config:

logger:
  default: info

additional_ca:
  badssl_ca: badssl-com-chain.pem

rest_command:
  additional_ca_test:
    url: "https://self-signed.badssl.com"
    method: get
    verify_ssl: true
    timeout: 30
